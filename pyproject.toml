[project]
name = "tofu-modules"
version = "0.1.0"
description = "OpenTofu modules developed for my projects."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
